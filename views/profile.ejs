<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="css/nav.css" />
    <style>
      /* Custom CSS for the profile page */
      body {
        background-color: #f8f9fa;
      }

      .container {
        margin-top: 50px;
      }

      .card {
        border: none;
      }

      .card-header {
        background-color: #007bff;
        color: white;
        text-align: center;
      }

      .card-body {
        background-color: #fff;
      }

      .btn-primary {
        background-color: #007bff;
        border: none;
      }

      .btn-primary:hover {
        background-color: #0056b3;
      }

      /* Responsive styles */
      @media (max-width: 768px) {
        /* Adjust container margin for smaller screens */
        .container {
          margin-top: 20px;
        }
      }

      /* Adjust table styles for small screens */
      @media (max-width: 576px) {
        .table th,
        .table td {
          font-size: 14px;
        }
      }
    </style>
  </head>
  <body>
    <nav>
      <h1>DishDelights</h1>
      <div>
        <a class="click" href="/logout">Logout</a>
      </div>
    </nav>

    <div class="container">
      <div class="row">
        <div class="col-md-8 offset-md-2">
          <div class="card">
            <div class="card-header">
              <h3>User Profile</h3>
            </div>
            <div class="card-body">
              <div class="table-responsive">
                <table class="table table-bordered">
                  <tr>
                    <th>First Name:</th>
                    <td><%= profileData.firstName %></td>
                  </tr>
                  <tr>
                    <th>Last Name:</th>
                    <td><%= profileData.lastName %></td>
                  </tr>
                  <tr>
                    <th>Email:</th>
                    <td><%= profileData.email %></td>
                  </tr>
                  <tr>
                    <th>Age:</th>
                    <td><%= profileData.age %></td>
                  </tr>
                </table>
              </div>
            </div>
            <div class="card-footer">
              <a href="/meals" class="btn btn-primary">Back to Home</a>
              <a href="/deleteProfile" class="btn btn-danger">Delete Profile</a>
              <a href="/editProfile" class="btn btn-warning">Update Profile</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
